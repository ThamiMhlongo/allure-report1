{
  "name": "cypress0708",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browser:chrome": "npx cypress run --browser=chrome --headless --spec cypress/e2e/test_cases/ --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.30.0",
    "cypress": "^13.13.2",
    "mocha-allure-reporter": "^1.4.0"
  }
}
